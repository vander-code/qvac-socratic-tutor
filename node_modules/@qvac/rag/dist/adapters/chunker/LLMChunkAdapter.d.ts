import { BaseChunkAdapter } from './BaseChunkAdapter.js';
import type { Doc, LLMChunkOpts } from '../../types.js';
export declare class LLMChunkAdapter extends BaseChunkAdapter {
    constructor(opts?: LLMChunkOpts);
    chunkText(input: string | string[], opts?: LLMChunkOpts): Promise<Doc[]>;
    private processChunks;
}
