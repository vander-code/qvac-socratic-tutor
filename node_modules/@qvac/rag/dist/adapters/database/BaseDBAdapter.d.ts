import ReadyResource from 'ready-resource';
import type { BaseDBAdapterConfig, DbOpts, EmbeddedDoc, ReindexOpts, ReindexResult, SaveEmbeddingsResult, SearchParams, SearchResult } from '../../types.js';
export declare abstract class BaseDBAdapter extends ReadyResource {
    isInitialized: boolean;
    constructor(config?: Record<string, unknown>);
    saveEmbeddings(docs: EmbeddedDoc[], opts?: DbOpts): Promise<SaveEmbeddingsResult[]>;
    deleteEmbeddings(ids: string[]): Promise<boolean>;
    search(query: string, queryVector: number[], params?: SearchParams): Promise<SearchResult[]>;
    reindex(opts?: ReindexOpts): Promise<ReindexResult>;
    getConfig(): Promise<BaseDBAdapterConfig | null>;
}
