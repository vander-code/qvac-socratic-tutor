import type { LoggerInterface } from '@qvac/logging';
import type { BaseDBAdapter } from '../adapters/database/BaseDBAdapter.js';
import type { ChunkingService } from './core/ChunkingService.js';
import type { EmbeddingService } from './core/EmbeddingService.js';
import type { BaseChunkOpts, Doc, EmbeddedDoc, IngestOpts, IngestResult, PartialDoc, SaveEmbeddingsOpts, SaveEmbeddingsResult } from '../types.js';
interface IngestionServiceConfig {
    dbAdapter: BaseDBAdapter;
    chunkingService: ChunkingService;
    embeddingService: EmbeddingService;
    logger?: LoggerInterface;
}
export declare class IngestionService {
    dbAdapter: BaseDBAdapter;
    chunkingService: ChunkingService;
    embeddingService: EmbeddingService;
    logger: LoggerInterface;
    constructor({ dbAdapter, chunkingService, embeddingService, logger }: IngestionServiceConfig);
    chunk(input: string | string[], opts?: BaseChunkOpts): Promise<Doc[]>;
    private validateEmbeddedDocs;
    saveEmbeddings(embeddedDocs: EmbeddedDoc[], opts?: SaveEmbeddingsOpts): Promise<SaveEmbeddingsResult[]>;
    ingest(docs: string | Array<string | PartialDoc>, embeddingModelId: string, opts?: IngestOpts): Promise<IngestResult>;
    deleteEmbeddings(ids: string[]): Promise<boolean>;
}
export {};
