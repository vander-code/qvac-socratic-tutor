import { registerPlugins } from './registry.js';
import * as hostApi from '../api/index.js';
export { registerPlugin, registerPlugins, getPlugin, getPluginHandler, getTurboVecIndexProvider, hasPlugin, getAllPlugins, clearPlugins, unregisterPlugin } from './registry.js';
/**
 * Register a set of plugins and return the host API bound to them. The
 * ergonomic entry point for assembling an explicit plugin subset — we ship
 * no default plugins, so an app registers exactly the engines it needs.
 */
export function plugins(pluginList) {
    registerPlugins(pluginList);
    return hostApi;
}
