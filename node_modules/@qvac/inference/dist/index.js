/// <reference types="bare" />
// Public API: the operations, engine lifecycle and explicit plugin assembly, and
// the value-clean surface (schemas / types / consts / errors / model registry)
// re-exported from `./surface`.
export { batchCompletion, completion, deleteCache, loadModel, downloadAsset, heartbeat, unloadModel, transcribe, transcribeStream, bciTranscribe, bciTranscribeStream, embed, finetune, translate, cancel, ragChunk, ragIngest, ragSaveEmbeddings, ragSearch, ragDeleteEmbeddings, ragReindex, ragListWorkspaces, ragCloseWorkspace, ragDeleteWorkspace, textToSpeech, textToSpeechStream, getModelInfo, getLoadedModelInfo, getSystemResources, assessModelFit, loggingStream, subscribeServerLogs, ocr, invokePlugin, invokePluginStream, diffusion, audioGen, classify, video, upscale, worldCreateScene, worldStep, modelRegistryList, modelRegistrySearch, modelRegistryGetModel, suspend, resume, state, vla, vlaHparams, vlaSetEmbodiment, vlaPreprocessImage, vlaPadState, VLA_DEFAULT_IMAGE_SIZE } from './api/index.js';
// Engine lifecycle and explicit plugin assembly. No plugins are registered by
// default: an app assembles the engines it needs via `plugins([...])` or the
// `registerPlugin`/`registerPlugins` primitives, then calls the operations.
export { close } from './dispatch.js';
export { plugins, registerPlugin, registerPlugins, getPlugin, hasPlugin, getAllPlugins, unregisterPlugin } from './plugins/index.js';
// The value-clean surface (also published as `@qvac/inference/surface`).
export * from './surface.js';
