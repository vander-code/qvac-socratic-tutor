import type { Tool, ToolCall, ToolCallError } from '../../schemas/index.js';
export type ParserResult = {
    matched: boolean;
    toolCalls: ToolCall[];
    errors: ToolCallError[];
};
export declare function stripThinkingBlocks(text: string): string;
export declare function generateStableToolCallId(name: string, args: Record<string, unknown>): string;
export declare function isValidToolCall(obj: unknown): obj is {
    name: string;
    arguments: Record<string, unknown>;
    id?: string;
};
export declare function validateToolArguments(toolName: string, args: Record<string, unknown>, tools: Tool[]): {
    isValid: boolean;
    error?: ToolCallError;
};
