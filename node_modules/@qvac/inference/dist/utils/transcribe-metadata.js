import { ModelType } from '../schemas/index.js';
import { TranscriptionFailedError } from '../errors/index.js';
/**
 * Normalize a native whisper-addon segment to the engine-level `TranscribeSegment`
 * shape exposed to callers: seconds → milliseconds, `toAppend` → `append`,
 * and defaults for optional fields.
 */
export function toTranscribeSegment(chunk) {
    return {
        text: chunk.text,
        startMs: (chunk.start ?? 0) * 1000,
        endMs: (chunk.end ?? 0) * 1000,
        append: chunk.toAppend ?? false,
        id: chunk.id ?? 0
    };
}
/**
 * Guard used by transcription ops when the caller opts into `metadata: true`.
 * Only the whisper engine emits per-segment metadata; any other engine
 * requesting metadata is rejected with `TranscriptionFailedError`.
 */
export function assertMetadataSupported(modelId, engineType, metadata) {
    if (!metadata)
        return;
    if (engineType !== ModelType.whispercppTranscription) {
        throw new TranscriptionFailedError(`metadata mode is not supported on model ${modelId} (engine: ${engineType || 'unknown'}); only the whisper engine supports metadata`);
    }
}
