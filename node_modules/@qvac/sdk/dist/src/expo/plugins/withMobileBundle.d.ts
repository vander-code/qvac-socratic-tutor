import type { ExpoConfig } from 'expo/config';
/**
 * Desktop-only modules deferred so bare-pack stops walking into them. They back
 * the advisory fit check, which mobile refuses at runtime, but the imports are
 * static and pull in `bare-process` -> `bare-posix`, which ships no
 * `android-arm64` prebuild and fails bundle verification.
 */
declare const MOBILE_UNSUPPORTED_MODULES: string[];
declare const MOBILE_HOSTS: string[];
type BareKitLinkerPaths = {
    android: string | null;
    ios: string | null;
};
/**
 * Expo plugin: bundle, verify, then copy the mobile worker bundle.
 *
 * Flow: bundleSdk -> verifyBundle -> copy to `<sdkPackageDir>/dist/worker.mobile.bundle.js`.
 * Uses `qvac.config.*` if present.
 */
declare function withMobileBundle(config: ExpoConfig): ExpoConfig;
/** Runs the patched iOS linker after bundleSdk has written the current addons manifest. */
declare function runIOSAddonLinker(linkerPath: string): Promise<void>;
/**
 * Patches react-native-bare-kit linkers to use the addons manifest and returns
 * the paths for each platform that was successfully patched.
 */
declare function patchBareKitLinkers(projectRoot: string, qvacSdkPath: string): BareKitLinkerPaths;
export { MOBILE_HOSTS, MOBILE_UNSUPPORTED_MODULES, patchBareKitLinkers, runIOSAddonLinker };
export type { BareKitLinkerPaths };
export default withMobileBundle;
//# sourceMappingURL=withMobileBundle.d.ts.map