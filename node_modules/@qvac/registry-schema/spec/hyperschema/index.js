// This file is autogenerated by the hyperschema compiler
// Schema Version: 2
/* eslint-disable camelcase */
/* eslint-disable quotes */
/* eslint-disable space-before-function-paren */

const { c } = require('hyperschema/runtime')

const VERSION = 2

// eslint-disable-next-line no-unused-vars
let version = VERSION

// @qvac-main-registry/model-blob-binding
const encoding0 = {
  preencode(state, m) {
    c.fixed32.preencode(state, m.coreKey)
    c.uint.preencode(state, m.blockOffset)
    c.uint.preencode(state, m.blockLength)
    c.uint.preencode(state, m.byteOffset)
    c.uint.preencode(state, m.byteLength)
    c.string.preencode(state, m.sha256)
  },
  encode(state, m) {
    c.fixed32.encode(state, m.coreKey)
    c.uint.encode(state, m.blockOffset)
    c.uint.encode(state, m.blockLength)
    c.uint.encode(state, m.byteOffset)
    c.uint.encode(state, m.byteLength)
    c.string.encode(state, m.sha256)
  },
  decode(state) {
    const r0 = c.fixed32.decode(state)
    const r1 = c.uint.decode(state)
    const r2 = c.uint.decode(state)
    const r3 = c.uint.decode(state)
    const r4 = c.uint.decode(state)
    const r5 = c.string.decode(state)

    return {
      coreKey: r0,
      blockOffset: r1,
      blockLength: r2,
      byteOffset: r3,
      byteLength: r4,
      sha256: r5
    }
  }
}

// @qvac-main-registry/license
const encoding1 = {
  preencode(state, m) {
    c.string.preencode(state, m.spdxId)
    c.string.preencode(state, m.name)
    c.string.preencode(state, m.url)
    c.string.preencode(state, m.text)
  },
  encode(state, m) {
    c.string.encode(state, m.spdxId)
    c.string.encode(state, m.name)
    c.string.encode(state, m.url)
    c.string.encode(state, m.text)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)
    const r2 = c.string.decode(state)
    const r3 = c.string.decode(state)

    return {
      spdxId: r0,
      name: r1,
      url: r2,
      text: r3
    }
  }
}

// @qvac-main-registry/model.blobBinding
const encoding2_4 = c.frame(encoding0)
// @qvac-main-registry/model.tags
const encoding2_9 = c.array(c.string)

// @qvac-main-registry/model
const encoding2 = {
  preencode(state, m) {
    const flags =
      (m.quantization ? 1 : 0) |
      (m.params ? 2 : 0) |
      (m.description ? 4 : 0) |
      (m.notes ? 8 : 0) |
      (m.tags ? 16 : 0) |
      (m.deprecated ? 32 : 0) |
      (m.deprecatedAt ? 64 : 0) |
      (m.replacedBy ? 128 : 0) |
      (m.deprecationReason ? 256 : 0) |
      ((version >= 2 && m.ggufMetadata) ? 512 : 0)

    c.string.preencode(state, m.path)
    c.string.preencode(state, m.source)
    c.string.preencode(state, m.engine)
    c.string.preencode(state, m.licenseId)
    encoding2_4.preencode(state, m.blobBinding)
    c.uint.preencode(state, flags)

    if (m.quantization) c.string.preencode(state, m.quantization)
    if (m.params) c.string.preencode(state, m.params)
    if (m.description) c.string.preencode(state, m.description)
    if (m.notes) c.string.preencode(state, m.notes)
    if (m.tags) encoding2_9.preencode(state, m.tags)
    if (m.deprecatedAt) c.string.preencode(state, m.deprecatedAt)
    if (m.replacedBy) c.string.preencode(state, m.replacedBy)
    if (m.deprecationReason) c.string.preencode(state, m.deprecationReason)
    if (version >= 2 && m.ggufMetadata) c.string.preencode(state, m.ggufMetadata)
  },
  encode(state, m) {
    const flags =
      (m.quantization ? 1 : 0) |
      (m.params ? 2 : 0) |
      (m.description ? 4 : 0) |
      (m.notes ? 8 : 0) |
      (m.tags ? 16 : 0) |
      (m.deprecated ? 32 : 0) |
      (m.deprecatedAt ? 64 : 0) |
      (m.replacedBy ? 128 : 0) |
      (m.deprecationReason ? 256 : 0) |
      ((version >= 2 && m.ggufMetadata) ? 512 : 0)

    c.string.encode(state, m.path)
    c.string.encode(state, m.source)
    c.string.encode(state, m.engine)
    c.string.encode(state, m.licenseId)
    encoding2_4.encode(state, m.blobBinding)
    c.uint.encode(state, flags)

    if (m.quantization) c.string.encode(state, m.quantization)
    if (m.params) c.string.encode(state, m.params)
    if (m.description) c.string.encode(state, m.description)
    if (m.notes) c.string.encode(state, m.notes)
    if (m.tags) encoding2_9.encode(state, m.tags)
    if (m.deprecatedAt) c.string.encode(state, m.deprecatedAt)
    if (m.replacedBy) c.string.encode(state, m.replacedBy)
    if (m.deprecationReason) c.string.encode(state, m.deprecationReason)
    if (version >= 2 && m.ggufMetadata) c.string.encode(state, m.ggufMetadata)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)
    const r2 = c.string.decode(state)
    const r3 = c.string.decode(state)
    const r4 = encoding2_4.decode(state)
    const flags = c.uint.decode(state)

    return {
      path: r0,
      source: r1,
      engine: r2,
      licenseId: r3,
      blobBinding: r4,
      quantization: (flags & 1) !== 0 ? c.string.decode(state) : null,
      params: (flags & 2) !== 0 ? c.string.decode(state) : null,
      description: (flags & 4) !== 0 ? c.string.decode(state) : null,
      notes: (flags & 8) !== 0 ? c.string.decode(state) : null,
      tags: (flags & 16) !== 0 ? encoding2_9.decode(state) : null,
      deprecated: (flags & 32) !== 0,
      deprecatedAt: (flags & 64) !== 0 ? c.string.decode(state) : null,
      replacedBy: (flags & 128) !== 0 ? c.string.decode(state) : null,
      deprecationReason: (flags & 256) !== 0 ? c.string.decode(state) : null,
      ggufMetadata: (version >= 2 && (flags & 512) !== 0) ? c.string.decode(state) : null
    }
  }
}

// @qvac-main-registry/model-key
const encoding3 = {
  preencode(state, m) {
    c.string.preencode(state, m.path)
    c.string.preencode(state, m.source)
  },
  encode(state, m) {
    c.string.encode(state, m.path)
    c.string.encode(state, m.source)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const r1 = c.string.decode(state)

    return {
      path: r0,
      source: r1
    }
  }
}

// @qvac-main-registry/writer
const encoding4 = {
  preencode(state, m) {
    c.fixed32.preencode(state, m.key)
  },
  encode(state, m) {
    c.fixed32.encode(state, m.key)
  },
  decode(state) {
    const r0 = c.fixed32.decode(state)

    return {
      key: r0
    }
  }
}

function setVersion(v) {
  version = v
}

function encode(name, value, v = VERSION) {
  version = v
  return c.encode(getEncoding(name), value)
}

function decode(name, buffer, v = VERSION) {
  version = v
  return c.decode(getEncoding(name), buffer)
}

function getEnum(name) {
  switch (name) {
    default:
      throw new Error('Enum not found ' + name)
  }
}

function getEncoding(name) {
  switch (name) {
    case '@qvac-main-registry/model-blob-binding':
      return encoding0
    case '@qvac-main-registry/license':
      return encoding1
    case '@qvac-main-registry/model':
      return encoding2
    case '@qvac-main-registry/model-key':
      return encoding3
    case '@qvac-main-registry/writer':
      return encoding4
    default:
      throw new Error('Encoder not found ' + name)
  }
}

function getStruct(name, v = VERSION) {
  const enc = getEncoding(name)
  return {
    preencode(state, m) {
      version = v
      enc.preencode(state, m)
    },
    encode(state, m) {
      version = v
      enc.encode(state, m)
    },
    decode(state) {
      version = v
      return enc.decode(state)
    }
  }
}

const resolveStruct = getStruct // compat

module.exports = {
  resolveStruct,
  getStruct,
  getEnum,
  getEncoding,
  encode,
  decode,
  setVersion,
  version
}
